import { DebouncedInputDirective } from './debounced-input.directive';

describe('DebouncedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DebouncedInputDirective();
    expect(directive).toBeTruthy();
  });
});
